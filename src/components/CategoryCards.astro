---
interface CategoryItem {
  title: string;
  text: string;
  href: string;
  image: string;
}

interface Props {
  categories: CategoryItem[];
}

const { categories } = Astro.props;
---

<div class="grid gap-5 md:grid-cols-3">
  {
    categories.map((item) => (
      <a href={item.href} class="group overflow-hidden rounded-card border border-border bg-card shadow-soft">
        <img
          src={item.image}
          alt={item.title}
          class="h-40 w-full object-cover transition duration-300 group-hover:scale-[1.02]"
          loading="lazy"
        />
        <div class="space-y-2 p-5">
          <h3 class="font-heading text-4xl">{item.title}</h3>
          <p class="text-lg text-muted">{item.text}</p>
        </div>
      </a>
    ))
  }
</div>
